# pure-orm
A lightweight, zero-magic ORM framework focused only on two things:  type-safe SQL generation via Java Lambda, and automatic result mapping.  No state management, no lazy loading, no cache—pure data access.
